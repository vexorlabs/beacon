# Beacon Backend Configuration
# Copy to .env and modify as needed

# Path to SQLite database (default: ~/.beacon/traces.db)
# BEACON_DB_PATH=~/.beacon/traces.db

# Backend port (default: 7474)
# BEACON_BACKEND_PORT=7474

# Log level (default: INFO)
# BEACON_LOG_LEVEL=INFO

# LLM API keys for replay (Phase 3)
# Required only if you use the Replay feature to re-run LLM calls
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...
# GEMINI_API_KEY=...   (alternative — either works)
