# Beacon Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.

# ─── Backend ──────────────────────────────────────────────
# Path to the SQLite database file (default: ~/.beacon/traces.db)
# BEACON_DB_PATH=~/.beacon/traces.db

# Port the backend listens on (default: 7474)
# BEACON_BACKEND_PORT=7474

# Log level for the backend (default: INFO)
# BEACON_LOG_LEVEL=INFO

# ─── SDK ──────────────────────────────────────────────────
# URL the SDK sends spans to (default: http://localhost:7474)
# BEACON_BACKEND_URL=http://localhost:7474

# Set to "false" to disable all SDK tracing without removing it from code
# BEACON_ENABLED=true

# Set to "false" to skip monkey-patching (manual instrumentation only)
# BEACON_AUTO_PATCH=true

# SDK internal log level (default: WARNING)
# BEACON_LOG_LEVEL=WARNING

# ─── LLM APIs (used by backend replay feature only) ───────
# Your OpenAI API key — required for replaying openai llm_call spans
# OPENAI_API_KEY=sk-...

# Your Anthropic API key — required for replaying anthropic llm_call spans
# ANTHROPIC_API_KEY=sk-ant-...
